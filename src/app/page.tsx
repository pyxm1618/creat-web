export default function HomePage() {
  return (
    <main className="shell">
      <section className="card" aria-labelledby="foundation-title">
        <p className="eyebrow">Private starter foundation</p>
        <h1 id="foundation-title">creat-web foundation</h1>
        <p>
          This neutral server-rendered page validates the reusable platform shell. Product-specific
          positioning and visual design are supplied by each downstream project.
        </p>
      </section>
    </main>
  );
}
